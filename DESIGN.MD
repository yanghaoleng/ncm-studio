# DESIGN

## 设计定位

- 网站正式展示名统一为“水下听歌大救星”；CLI/npm 包名继续使用 `ncm-studio-cli`。
- 工具感优先：信息清晰、操作直接，不做营销型首屏。
- 主工作区采用单列布局，音乐文件处理面板占据可用宽度，空状态保持紧凑。
- 整体使用白/深灰中性底色，蓝色用于主操作，黄色用于等待警告，红色用于错误。
- 标题字体使用 `Avenir Next / Gill Sans / PingFang SC`，正文使用系统无衬线字体。

## 可复用参数

- 面板圆角：14-18px；按钮圆角：10px；紧凑提示圆角：10px。
- 常规交互过渡：180ms ease；状态颜色过渡：260ms ease。
- 按钮高度：最小 42px；图标通常为 16-18px。
- 标题下方的支持格式提示使用 12px、左对齐并允许自动换行。
- 空状态标题区与文件拖入区使用单层 8px 间距，避免标题栏下边距重复叠加。

## ZIP 打包状态

- 打包按钮文字显示实时整数百分比，并用从左向右增长的底色表示进度。
- 打包中按钮保持 100% 不透明，光标为 `wait`，避免误解为不可用。
- 卡顿提示使用 `--yellow-soft`，失败提示使用 `--rose-soft`，均使用 12px 文字和 17px 警告图标。
- 进度条宽度过渡为 180ms ease；遵循 `prefers-reduced-motion`。

## 封面一致性

- 列表缩略图、播放器预览和下载 MP3 使用同一份 NCM 封面数据。
- MP3 封面写入标准 ID3v2.3 `APIC` 前封面帧，ZIP 打包不得替换为未写标签的原始音频。
- 文件会在添加后自动转换，队列顶部不再显示冗余的“全部转换”按钮；“继续添加”与“打包下载”保持主要操作层级。
- 文件行保留体积与进度信息，不重复显示统一输出格式的 MP3 标签。

## CLI 交互

- 人类模式使用简短中文进度：`[当前/总数] 百分比 输入 -> 输出`。
- 机器模式保持 stdout 为单个 JSON 对象，进度只写入 stderr。
- 成功退出码为 0，参数、输入或任何文件转换失败时为 1。

## CLI 安装窗口

- 桌面端工作区使用主面板 + 310px 右侧窗口，间距 14px；920px 以下改为单列。
- 窗口使用 14px 圆角和中性表面，绿色仅用于终端图标和复制成功反馈。
- CLI 板块默认只显示标题和下拉箭头；展开后展示适用场景、AI 操作步骤、示例提示词和安装链接。
- CLI 详情正文与打赏说明统一为 15px；打赏栏目展开时自动收起 CLI，避免右栏信息同时过长。
- CLI 标题在中文、英文和日文下均保持左对齐，英文使用 “AI agent” 表述。
- 安装地址行本身是唯一复制控件，“安装链接”作为框内前缀，只复制 npm 地址。
- 成功后复制图标短暂变为绿色对钩，2.2s 后恢复；不显示额外操作行，不改变布局。

## 打赏模块

- 位于右栏底部，首次只显示标题与下拉箭头；展开后直接显示说明和二维码，不增加中间按钮。
- 展开后使用支付宝/微信双标签，默认微信；选中图标分别使用支付宝蓝与微信绿。
- 二维码保持方形裁切尺寸，使用 WebP 质量 90 并通过解码验证；白底、10px 内边距、12px 圆角，展开动效 220ms ease-out。
- ZIP 打包成功后自动展开打赏栏目；二维码使用 2px 低饱和彩色描边，7.5s 往返轮换并遵循 `prefers-reduced-motion`。
- 打赏说明比侧栏普通小字大 2px；微信号复制入口为无边框、无底色的下划线文字，成功 Toast 固定在底部居中。
- 打赏语气保持自由、无压力，明确“可以随意打赏，支持后续维护”，不限定咖啡金额。
- 随机祝福优先使用轻松、俏皮、贴近日常的表达，例如不加班、耳机满电、好运在线；避免过度抒情或过于正式。
- 随机感谢语使用 `animate-text` 的 `spring-scale-in`：按词入场，采用网站预设换算后的 259ms、68ms 错峰、`cubic-bezier(0.34, 1.56, 0.64, 1)`；每次展开仅播放一次，减少动态偏好下直接显示静态文案。

## 页脚与使用说明

- 页脚左侧并排放置“网站使用说明”和作者主页；说明文字后配圆形感叹号图标，点击打开居中的可关闭小窗。
- “网站使用说明”入口不使用下划线；弹窗底部语言选项使用轻量文字按钮，当前语言以正文色标识，点击后原地更新弹窗与页面文案。
- 两个页脚入口统一使用 13px、字重 750 的文字，图标统一为 14px，图文间距统一为 6px。

## SEO 内容区

- 后续新增的搜索说明与常见问题应位于主工具之后，保持工具优先，不改造成营销型首屏。
- 关键词必须自然写入标题、说明和问答，禁止堆砌或用隐藏文字承载。
- 搜索引擎所有权验证文件属于基础设施，不参与页面视觉呈现。
- 第一轮 SEO 文案应等待 Bing 搜索表现数据回填后确定主次关键词。
- 中文应用页的 SEO 说明收进“网站使用说明”小窗；英文和日文静态落地页继续保持可直接抓取。
